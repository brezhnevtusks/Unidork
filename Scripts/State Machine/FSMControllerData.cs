namespace Unidork.StateMachine
{
	/// <summary>
	/// Base class for objects that hold data for a <see cref="FSMController"/>. 
	/// Required to allow the state controller to return data using generics.
	/// </summary>
	public abstract class FSMControllerData { }
}